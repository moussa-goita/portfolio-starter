import React from 'react'

const Card = (Emoji, heading, detail) => {
  return (
    <div className="card">
        <img src="" alt="" />
        <span>{heading}</span>
        <span>{detail}</span>
    </div>
  )
}

export default Card